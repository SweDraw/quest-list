import React from 'react';
import { BrowserRouter as Router, Switch } from 'react-router-dom';

const App: React.FC = () => {
  return (
    <Router>
      <Switch></Switch>
    </Router>
  );
};

export default App;
